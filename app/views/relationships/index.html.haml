%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @following_relationships.each do |following_relationship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to following_relationship.leader.full_name, following_relationship.leader
          %td.extra-padding= following_relationship.leader.queue_items.count
          %td.extra-padding= following_relationship.leader.followers.count
          %td.extra-padding
            = link_to relationship_path(following_relationship), method: :delete do
              %i.glyphicon.glyphicon-remove
